machine:
  environment:
    PROJECT_NAME: react
    CLOUDSDK_COMPUTE_ZONE: us-east1-b
    PROJECT_ID: celestial-brand-187707
  node:
    version: 8.9.2
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
deployment:
    staging:
        branch: master
        # deploy to compute Engine
   
    
