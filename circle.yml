machine:
  node:
    version: 8.9.2
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
deployment:
    staging:
        branch: master
        commands:
        # deploy tocompute Engine
        - gcloud -q preview app deploy app.yaml --promote --version=1    
    
