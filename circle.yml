machine:
  node:
    version: 8.9.2
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
deployment:
    staging:
        branch: master
        commands:
        # deploy to compute Engine
       sudo gcloud compute --project "celestial-brand-187707" ssh --zone "us-east1-b" "react"

   
    
