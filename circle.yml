machine:
  node:
    version: 8.9.2
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
deployment:
    staging:
        branch: master
        # deploy to compute Engine
        gcloud compute --project "celestial-brand-187707"

   
    
